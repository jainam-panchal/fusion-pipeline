rejected: "node `ab_run`: pattern `(?<x>(?:a|b)*)(?=c)`: canary tripped: work limit exceeded across start positions on unanchored 'a' repeated input of 1024 bytes (match limit 1000000, work limit 65536)"
