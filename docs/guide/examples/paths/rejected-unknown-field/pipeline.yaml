source:
  type: nats
  stream: LOGS
  consumer: pipeline
nodes:
  - id: keep_errors
    type: filter
    condition: severity == "ERROR"
    action: keep
  - id: out
    type: sink.nats
    stream: PROCESSED
    subject: processed.logs
