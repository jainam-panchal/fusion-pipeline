source:
  type: nats
  stream: LOGS
  consumer: pipeline
nodes:
  - id: out
    type: sink.nats
    stream: PROCESSED
    subject: processed.logs
  - id: drop_debug
    type: filter
    condition: severity_text == "DEBUG"
    action: drop
