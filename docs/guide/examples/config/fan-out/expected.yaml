acks: [ack, ack]
sinks:
  all:
    - payload: {severity_text: ERROR, body: disk full}
    - payload: {severity_text: INFO, body: user logged in}
  errors_out:
    - payload: {severity_text: ERROR, body: disk full}
