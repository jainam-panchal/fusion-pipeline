acks: [ack, ack, ack]
sinks:
  linux_out:
    - payload: {body: a, resource: {log.format: Linux}}
  apache_out:
    - payload: {body: b, resource: {log.format: Apache}}
  other_out:
    - payload: {body: c, resource: {log.format: Mac}}
