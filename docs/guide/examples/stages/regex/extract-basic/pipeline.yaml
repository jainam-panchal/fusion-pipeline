nodes:
  - id: parse
    type: extract
    field: body
    pattern: '^(?<level>[A-Z]+) (?<service>\w+)(?: pid=(?<pid>\d+))?: (?<message>.+)$'
  - id: out
    type: sink.nats
    stream: PROCESSED
    subject: processed.logs
