source:
  type: nats
  stream: LOGS
  consumer: pipeline
nodes:
  - id: enrich
    type: lua
    limits: {memory_kib: 8}
    source: |
      function process(record) return record end
  - id: out
    type: sink.nats
    stream: PROCESSED
    subject: processed.logs
